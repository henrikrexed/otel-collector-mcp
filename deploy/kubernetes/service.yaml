apiVersion: v1
kind: Service
metadata:
  name: otel-collector-mcp
  namespace: otel-collector-mcp
  labels:
    app.kubernetes.io/name: otel-collector-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: otel-collector-mcp
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
      protocol: TCP
